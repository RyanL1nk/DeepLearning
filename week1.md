# 神经网络与深度学习课程第一次总结

## 一、多层前馈神经网络（BP网络）

### 1.1 XOR 问题与多层感知机（MLP）

#### XOR 是线性不可分问题：
- 单层感知机无法拟合 XOR。
- 需要引入隐藏层构建 **三层网络**（输入层-隐藏层-输出层）：

$$
\begin{aligned}
y_1^{(1)} &= f(w_{11}^{(1)} x_1 + w_{12}^{(1)} x_2 - \theta_1^{(1)}) \\\\
y_2^{(1)} &= f(w_{21}^{(1)} x_1 + w_{22}^{(1)} x_2 - \theta_2^{(1)}) \\\\
y &= f(w_1^{(2)} y_1^{(1)} + w_2^{(2)} y_2^{(1)} - \theta)
\end{aligned}
$$

- 常用激活函数 $f(\cdot)$ 为：

$$
f(x) =
\begin{cases}
1, & x \ge 0 \\
0, & x < 0
\end{cases}
$$

---

### 1.2 MLP 逼近能力

- 定理 1：三层感知器可逼近任何**二值逻辑函数**；
- 定理 2：使用 S 型激活函数（如 Sigmoid、Tanh），可以逼近任何**连续函数**。

---

## 二、BP 算法（误差反向传播）

### 2.1 过程分为两步：

#### 正向传播（Forward）：
输入 → 隐层 → 输出，计算预测结果 $\hat{y}$。

#### 反向传播（Backward）：
计算损失函数
$\mathcal{L}$，使用 **梯度下降法** 更新参数。

### 2.2 常用损失函数：

- 均方误差（MSE）：

$$
\mathcal{L} = \frac{1}{2} \sum_{i}(y_i - \hat{y}_i)^2
$$

### 2.3 权值更新公式（链式法则）：

对于输出层：

$$
\delta_i^{(L)} = a_i^{(L)} (1 - a_i^{(L)}) (y_i - \hat{y}_i)
$$

对于隐含层：

$$
\delta_i^{(l)} = a_i^{(l)} (1 - a_i^{(l)}) \sum_j \delta_j^{(l+1)} w_{ji}^{(l+1)}
$$

更新权重：

$$
\Delta w_{ij}^{(l)} = \alpha \cdot \delta_i^{(l)} \cdot a_j^{(l-1)}
$$

---

## 三、卷积神经网络（CNN）

### 3.1 核心组件

#### 卷积层（Convolution）：

$$
z^{(l)}(x, y) = \sum_{u=0}^{p} \sum_{v=0}^{q} a^{(l-1)}(x+u, y+v) \cdot w^{(l)}(u,v)
$$

#### 池化层（Pooling）：
最大池化（Max Pool）或平均池化（Avg Pool）：

$$
a^{(l)}(x, y) = \max_{i,j \in \text{region}} a^{(l-1)}(x+i, y+j)
$$

---

### 3.2 CNN反向传播简要公式

#### 卷积层的梯度：

$$
\frac{\partial a^{(l+1)}(x_i, y_i)}{\partial w^{(l)}(u,v)} = \sum_{u,v} \frac{\partial a^{(l+1)}(x_i, y_i)}{\partial a^{(l)}(x_i + u, y_i + v)} \cdot \frac{\partial a^{(l)}(x_i + u, y_i + v)}{\partial w^{(l)}(u,v)}
$$

#### 池化层误差传递：
- 最大池化：误差传递给最大值所在位置；
- 平均池化：误差均分传递。

---

## 四、经典卷积神经网络结构

### 4.1 LeNet-5
- 输入： $28\times28$
- 结构：`Conv (6×5×5)` → `AvgPool` → `Conv (16×5×5)` → `AvgPool` → `FC (120→84→10)`
- 激活函数：Sigmoid / Tanh

### 4.2 AlexNet
- 使用 ReLU 激活函数；
- 最大池化；
- Dropout 防止过拟合；
- 双 GPU 训练支持。

### 4.3 VGG-16
- 特点：全部使用 $3 \times 3$ 卷积核；
- 层次深，参数超多（1.38亿）；
- 结构规整，便于迁移学习。

### 4.4 ResNet（残差网络）
- 引入 **残差连接（shortcut）** 解决梯度消失问题；
- 使网络层数可达数百层。

---

## 五、常用数据集

| 数据集名称     | 特点                                      |
|----------------|-------------------------------------------|
| **MNIST**      | 手写数字（0~9），灰度图，$28 \times 28$     |
| **Fashion-MNIST** | 衣物图像分类，结构与MNIST相同              |
| **CIFAR-10**   | 10类彩色图像，$32 \times 32$              |
| **PASCAL VOC** | 检测、分割、识别任务                       |
| **MS COCO**    | 包含 80 类复杂场景，图像数量超 30 万         |
| **ImageNet**   | 最权威的大型图像分类数据集（1000类）       |

---

## 六、本周重点知识点总结

- **MLP 可解决非线性问题，需借助 BP 算法学习参数；**
- **CNN 可用于图像处理，依靠局部连接、共享权重、池化降低复杂度；**
- **经典结构不断演化：LeNet → AlexNet → VGG → ResNet；**
- **PyTorch 框架学习实践基础，掌握张量与计算图使用；**
- **常用数据集的特点与应用场景需了解，支持实验与研究。**
